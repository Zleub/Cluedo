type Story {
	id: Int
	personae(name: String): [ Personae ]
	modsList: [ String ]
}

type Personae {
	_name: String
	knowledge(
		personae: String,
		actor: String,
		action: String,
		target: String
	): String
}

type Query {
	story(id: Int): Story
}
